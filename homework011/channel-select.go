package homework011

import (
	"fmt"
)

// スライスの値の合計値をチャネルに渡す関数
// 中身を適宜変更してください。引数、メソッド、関数名も変更して大丈夫です。
func sendSum() {
}

// 0~5までの値をループで回して偶数・奇数に分けてそれぞれの値を別々のチャネルに渡す関数
// 中身を適宜変更してください。引数、メソッド、関数名も変更して大丈夫です。
func sendValue() {
}

// それぞれ受けたチャネルの値をselect文で処理して出力する関数
// 中身を適宜変更してください。引数、メソッド、関数名も変更して大丈夫です。
func receiveValue() {
}

func Task4() {
	fmt.Println("homework011_4")
	// s := []int{1, 2, 3, 4, 5}
	// sum := make(chan int)
	// even := make(chan int)
	// odd := make(chan int)
	// quit := make(chan bool)

}
